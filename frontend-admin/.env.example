# Local admin frontend environment
VITE_APP_ENV=local
VITE_APP_NAME=Sivan Payments Admin
VITE_API_BASE_URL=http://localhost:3000

# Do not put ADMIN_API_KEY here for production static builds.
# Enter the admin key in the admin UI so it is stored locally in your browser.
